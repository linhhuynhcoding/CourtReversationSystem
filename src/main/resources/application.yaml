spring:
  application:
    name: CourtReservationSystem
  datasource:
    url: jdbc:postgresql://172.17.0.2:5432/postgres
    username: postgres
    password: cuoctinhdivang
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
        ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true